<%if  current_user.present?%>
   CURRENT USER <%= current_user.email.inspect %>
    ===============================================================================================================================
    Session<%= session.inspect %>
    ===============================================================================================================================
<% end %>
<%#= debug(params)%>
