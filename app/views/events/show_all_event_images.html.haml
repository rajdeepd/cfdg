%div.content.wrapper
  %h1.label-17{:style => "padding-bottom:10px;"}
    = @event.title
  -if @all_event_images.present?
    - @all_event_images.each do |i|
      .eventImageHolder
        = image_tag(i.image)